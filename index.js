function inserir() {
    var matricula = parseInt(prompt('Digite a matrícula:'));
    var nome = prompt('Digite o nome:');
    var faltas = parseInt(prompt('Digite o número de faltas'));
    var notaB1 = parseFloat(prompt('Digite a nota B1'))
    var notaB2 = parseFloat(prompt('Digite a nota B2'))
    var media = (notaB1 + notaB2) / 2;
    console.log(media);

    else (faltas > 25)
    if (media >7)
    alert (prompt)
}